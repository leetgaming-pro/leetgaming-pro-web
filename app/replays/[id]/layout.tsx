"use client";

/**
 * Replay Detail Layout
 * Simple wrapper - no RootLayout here since it's already in app/layout.tsx
 */
export default function ReplayDetailLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
